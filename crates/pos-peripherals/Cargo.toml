[package]
name = "pos-peripherals"
version = "1.6.7"
edition = "2021"
description = "Hardware peripherals abstraction (Receipt printing, drawer kick, scale integration)"

[dependencies]
pos-core = { path = "../pos-core" }
