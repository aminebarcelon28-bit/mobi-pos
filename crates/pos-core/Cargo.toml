[package]
name = "pos-core"
version = "1.6.8"
edition = "2021"
description = "Headless domain core, POS math, data contracts, and PosDb trait (Zero Tauri imports)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
