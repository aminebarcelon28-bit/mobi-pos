{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only: auto-updater + process (gated #[cfg(desktop)] in lib.rs)",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:default", "core:default"]
}
